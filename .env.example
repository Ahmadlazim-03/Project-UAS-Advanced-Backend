# Environment Variables for Vercel Deployment

## Backend Environment Variables (Required)
# Add these in Vercel Dashboard -> Settings -> Environment Variables

# Database - PostgreSQL (Railway)
DB_HOST=your-railway-postgres-host.railway.app
DB_USER=postgres
DB_PASSWORD=your-postgres-password
DB_NAME=railway
DB_PORT=5432

# Database - MongoDB (Railway)
MONGO_URI=mongodb://mongo:your-password@your-railway-mongo-host.railway.app:27017/achievement_db

# JWT Secret
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server
PORT=3000
GIN_MODE=release

## Frontend Environment Variables (Optional)
# These are automatically set by Vercel
# PUBLIC_API_URL=/api/v1

## How to set environment variables in Vercel:
# 1. Go to your project in Vercel Dashboard
# 2. Navigate to Settings -> Environment Variables
# 3. Add each variable above
# 4. Select "Production", "Preview", and "Development" for each
# 5. Click "Save"
# 6. Redeploy your application

## Note:
# - Never commit .env file to git
# - Use different values for production and development
# - Keep your secrets secure

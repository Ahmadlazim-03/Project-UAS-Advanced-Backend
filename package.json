{
  "scripts": {
    "dev": "go run main.go",
    "build": "go build -o bin/main .",
    "start": "go run main.go",
    "test": "go test ./...",
    "build:frontend": "cd frontend && npm install && npm run build",
    "deploy:all": "npm run build:frontend && railway up",
    "setup": "go mod download && cd frontend && npm install"
  }
}
